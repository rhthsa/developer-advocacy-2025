= Setup
include::_attributes.adoc[]

[#setup]
== Register to access the lab

* Get the lab registration URL and password from instructor.
* Register with your email address and the given password to access the lab.
+
image::setup/register-01.png[]

== Log in to OpenShift web console

* Open the OpenShift web console URL from the registration page in a web browser.
+
image::setup/register-02.png[]
+
* Log in with username and password get from the registration page.
+
image::shipwright/shp-1.png[]

* If it's the first time you log in to web console, the quick tour dialog will show up, just click Skip tour or close the dialog.
+
image::shipwright/shp-2.png[]

* Switch to *Developer* perspective (if it's not) from the drop-down list near the hamburger menu. Then click on *userX-shipwright* project where *userX* is your username such as `user1`.
+
image::shipwright/shp-3.png[]

== [Optional] Set theme color for console

* Click on your user name at the top right of OpenShift web console, then select *User References*.
+
image::shipwright/shp-4.png[]

* In the *General* section, you can change theme from Dark mode (default) to Light mode.
+
image::shipwright/shp-5.png[]

[#deploycontainerimage]
== Deploy Container Image

* Click *+Add* menu on the left, the *Add* page will show up then select *Container images* tile.
+
image::shipwright/shp-6.png[]

* Select *Image name from external registry* option.
* Enter `quay.io/voravitl/frontend-js:latest` then wait for OpenShift validates the container image registry.
* Change *Runtime icon* to `nodejs`.
* Set *Application name* to `frontend-js`.
* Set *Name* to `frontend-js`.
+
image::shipwright/shp-7.png[]

* Set *Resource type* to `Deployment`.
* Leave the *Target port* as default (`8080`)
* Make sure the *Create a route* checkbox is selected.
* Finally, click on the *Create* button. at the bottom.
+
image::shipwright/shp-8.png[]

* You will be redirected back to the *Topology* view. Wait until the Pod change to `Running`.
+
image::shipwright/shp-9.png[]

[#accesstheapplication]
== Access The Application

* Click on `frontend-js` Deployment icon then click on the arrow to open the web application.
+
image::shipwright/shp-10.png[]

* Make sure you can see the response from the  `frontend-js` application similar to the screenshot.
+
image::shipwright/shp-11.png[]


Now you can see that We can deploy container images via OpenShift console. It's easy!, right? But what if you don't have container image?

* How to build container image if you only have source code?
* How to build container image from your Dockerfile or Containerfile you've written?
* How to prepare tools, environment, etc. to build container image?

[#shipwright]
== Red Hat OpenShift Builds

image::shipwright/shipwright-logo.png[]

OpenShift Builds is an extensible build framework based on the https://shipwright.io/[Shipwright] project, which you can use to build container images on an OpenShift Container Platform cluster. You can build container images from source code and Dockerfiles by using image build tools, such as https://github.com/openshift/source-to-image[Source-to-Image (S2I)] and https://buildah.io/[Buildah]. You can create and apply build resources, view logs of build runs, and manage builds in your OpenShift Container Platform.

OpenShift Builds includes the following capabilities:

* Standard Kubernetes-native API for building container images from source code and Dockerfiles
* Support for Source-to-Image (S2I), Buildah, Buildpacks, etc.
* Extensibility with your own custom build strategies
* Execution of builds from source code in a local directory
* Shipwright CLI for creating and viewing logs, and managing builds on the cluster
* Integrated user experience with the Developer perspective of the OpenShift Container Platform web console

OpenShift Builds consists of the following custom resources (CRs):

* Build
* BuildStrategy and ClusterBuildStrategy
* BuildRun

Get More information

* https://docs.redhat.com/en/documentation/builds_for_red_hat_openshift/1.4/[Builds for Red Hat OpenShift 1.4]
* https://github.com/shipwright-io/build[Shipwright]
* https://developers.redhat.com/articles/2021/06/14/project-shipwright-and-future-red-hat-openshift-builds#[Project Shipwright and the future of Red Hat OpenShift builds]

[#shipwrightbuidah]
== Build and Deploy Application using Shipwright and Buildah

=== Access To Git Repository

In this lab, our application is hosted on a private Git repository. Follow these step to get access to the *gitea* Git repository server.

* Click on the command prompt icon at the top right corner of OpenShift web console to open the web terminal.
+
image::shipwright/shp-12.png[]

* If you open the web terminal first time, it'll ask for the project used to initialize the terminal. Select the `userX-shipwright` project where `userX` is your username, and then click *Start* button.
+
image::shipwright/shp-13.png[]

* Wait until the terminal is ready and command line prompt is shown.
+
image::shipwright/shp-14.png[]

* Run this command to get *gitea* URL.
+
[.console-input]
[source,adoc]
----
oc get route gitea -n gitea
----
+
sample output.
+
[.console-input]
[source,adoc]
----
NAME    HOST/PORT                                                      PATH   SERVICES   PORT    TERMINATION     WILDCARD
gitea   gitea-gitea.apps.cluster-bnb6v.bnb6v.sandbox1016.opentlc.com          gitea      <all>   edge/Redirect   None
----

* Copy the gitea DNS hostname and open it with a web browser.
+
image::shipwright/shp-15.png[]

* Log in with your username, and use `openshift` as password.
+
image::shipwright/shp-16.png[]

* Select `simple-rest-go` repository.
+
image::shipwright/shp-17.png[]

=== Review the Container Image Build Instruction

* Open the `Dockerfile`, then review build steps in the `Dockerfile` file.
+
image::shipwright/shp-18.png[]
+
image::shipwright/shp-19.png[]

=== Build and Deploy Container

* In gitea, back to the main `simple-rest-go` repository page, click on *<> Code* button then click copy button to copy the repository URL.
+
image::shipwright/shp-20.png[]

* Go to OpenShift web console then click *+Add* menu on the left. Select *Git Repository - Import from Git* tile.
+
image::shipwright/shp-21.png[]

On the *Import from Git* form, follow these steps:

* Enter the Git repo URL you've copied to *Git Repo URL* input.
* The wizard will detect `Dockerfile`` automatically.
* Select `Create Application` for the  *Application* drop-down list.
* Set *Application name* to `simple-rest-go`.
* Set *Name* to `simple-rest-go`.
+
image::shipwright/shp-22.png[]

* Select `Builds for OpenShift (Shipwright)` for *Build option*.
* Select `Buildah` for *Cluster Build Strategy*.
* Leave other configurations as default, then click *Create* button.
+
image::shipwright/shp-23.png[]

=== View Build Configuration and Build Logs

* In the Topology view, select `simple-rest-go` Deployment, go to *BuildRuns* section, then click *View logs* link.
+
image::shipwright/shp-24.png[]

* You should be able to see container image builds logs similar to the screenshot.
+
image::shipwright/shp-25.png[]

* Wait until the build status changes from Running to Succeded.
+
image::shipwright/shp-26.png[]

* Select *Builds* menu on the left, then click on *Shipwright Builds* tab to view Shipwright Build configuration.
+
image::shipwright/shp-27.png[]

* Select *Topology* menu on the left, select `simple-rest-go` Deployment, go to *BuildRuns* section again you'll see the build status shown as `Succeeded`.
+
image::shipwright/shp-28.png[]

=== Access To Application

* Click on the arrow icon to open the application URL.
+
image::shipwright/shp-29.png[]

* Ensure you can see the response from the application.
+
image::shipwright/shp-30.png[]

[#shipwrights2i]
== Build and Deploy Application with Shipwright and Source-to-Image (S2I)

=== Review Application Source Code.

* Go to *gitea* web console then open the `backend_quarkus` repository.
+
image::shipwright/shp-31.png[]
image::shipwright/shp-32.png[]

* Open the `code` folder to view the application source code. It's a Java application based on Quarkus framework. The APIs that application exposes are in `src/main/java/com/example/quarkus/BackendResource.java` file.
+
image::shipwright/shp-34.png[]
image::shipwright/shp-35.png[]

=== Build and Deploy Container

* In gitea, back to the main `backend_quarkus` repository page, click on *<> Code* button then click copy button to copy the repository URL.
+
image::shipwright/shp-33.png[]

* Go to OpenShift console, click `+Add` menu on the left, then select *Git Repository - Import from Git* tile.
+
image::shipwright/shp-21.png[]

On the *Import from Git* form, follow these steps:

* Enter the `backend_quarkus` Git repository URL you've copied to *Git Repo URL* input.
* Enter `/code` in *Context dir* input.
+
image::shipwright/shp-36.png[]

* The wizard will automatically detect Builder image for Java application. Click *Edit Import Strategy* link to choose Builder Image.
+
image::shipwright/shp-37.png[]

* Click on *Java* tile and select *openjdk-21-ubi9* for the Builder Image version.
+
image::shipwright/shp-38.png[]

* Select `Create Application` for the  *Application* drop-down list.
* Enter `backend-quarkus` for *Application name*.
* Enter `backend-quarkus` for *Name*.
* Select `Builds for OpenShift (Shipwright)` for *Build Option*.
* Select `Source-to-Image` for *Cluster Build Strategy*.
+
image::shipwright/shp-39.png[]

* Leave other configurations as default, then click *Create* button.
+
image::shipwright/shp-40.png[]

=== View Build Configuration and Build Logs

* On the Topology page, select `backend-quarkus` Deployment, go to *BuildRuns* section, then click *View logs* link.
+
image::shipwright/shp-41.png[]

* You should be able to see builds logs. Wait for the build status changes to `Successed`.
+
image::shipwright/shp-42.png[]
+
image::shipwright/shp-43.png[]

* Select *Builds* menu on the left, then click on *Shipwright Builds* tab to view Shipwrite Build configuration.
+
image::shipwright/shp-44.png[]

* Select *Topology* menu on the left, then select `backend-quarkus` Deployment, go to *BuildRuns* section again you'll see the build status shown as `Succeeded`.

=== Access To Application

* Click on the arrow icon of `backend-quarkus` Deployment to open the application URL.
+
image::shipwright/shp-45.png[]

* Ensure you can see response from the application similar to the screenshot.
+
image::shipwright/shp-46.png[]

=== Re-build and Re-deploy Application when Code Changes

* Go back to the *gitea* web console. In the `backend_quarkus` repository, open the `BackendResource.java` file in `code/src/main/java/com/example/quarkus` directory.
+
image::shipwright/shp-47.png[]

* Click on the pencil icon to edit file.
+
image::shipwright/shp-48.png[]

* Go to line number `201`, change the `+Backend version...+` to `+Backend OpenShift version...+`
+
image::shipwright/shp-49.png[]

* Enter the commit message then click *Commit Changes* button.
+
image::shipwright/shp-50.png[]

* Go back to OpenShift console. Select *Builds* menu on the left then click on *Shipwright Builds* tab. Click on action menu of `backend-quarkus` build configuration then select *Start*.
+
image::shipwright/shp-51.png[]

* The build process will be started. Wait until the build status changes to `Succeeded`.
+
image::shipwright/shp-52.png[]
+
image::shipwright/shp-53.png[]
+
image::shipwright/shp-54.png[]

* Open the application URL again, you should see the response message from the application change to `+Backend OpenShift version...+`
+
image::shipwright/shp-55.png[]

[#summary]
== Summary

With Shipwright, developers get a simplified approach for building container images, by defining a minimal YAML that does not require any previous knowledge of containers or container tooling. All you need is your source code in git and access to a container registry.

Shipwright supports any tool that can build container images in Kubernetes clusters, such as:

* https://github.com/GoogleContainerTools/kaniko[Kaniko]
* https://buildpacks.io/[Cloud Native Buildpacks]
* https://github.com/moby/buildkit[BuildKit]
* https://buildah.io/[Buildah]
* https://github.com/openshift/source-to-image[S2I]
